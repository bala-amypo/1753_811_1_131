# Application name
spring.application.name=demo1

# IMPORTANT: Do not change port (used by preview/nginx)
server.port=9001

# Required for reverse proxy (preview URL)
server.forward-headers-strategy=framework


# ================================
# DATABASE (DISABLED FOR PREVIEW)
# ================================
# Preview container-la MySQL run aagala
# So DB config temporary-aa disable pannirukkom

# spring.datasource.url=jdbc:mysql://localhost:3306/smart_grid?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=Amypo
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false


# ================================
# SWAGGER / OPENAPI
# ================================
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.enabled=true


# ================================
# LOGGING (optional, helpful)
# ================================
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=OFF
